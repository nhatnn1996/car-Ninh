'use client';
import { usePathname } from 'next/navigation';
import { useEffect, useRef } from 'react';

const Scroll = () => {
  // const fistLoad = useRef(false);
  // const pathname = usePathname();
  // useEffect(() => {
  //   if (fistLoad.current === false) {
  //     fistLoad.current = true;
  //     return;
  //   }
  //   // window.document.scrollingElement?.scrollTo({ top: 0, behavior: undefined });
  // }, [pathname]);
  return null;
};

export default Scroll;
